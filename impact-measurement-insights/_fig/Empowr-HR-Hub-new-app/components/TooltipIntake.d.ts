import * as React from 'react';
export interface TooltipIntakeProps {
  className?: string;
  style?: React.CSSProperties;
}
export declare const TooltipIntake: React.FC<TooltipIntakeProps>;
export default TooltipIntake;
